#include <iostream>
using namespace std;
const int MAX = 100;

int stack(){
    int stack_init[MAX];
    int z = -1;
    int top = z;
    int header = z;

    return 0;
}

void pda(string str){
    int state[MAX] = {0,1,2};
//    int string_int[MAX];
    bool flag = true;
    for(int i=0;i<=str.length();i++){
        if (str[i]=='1'){

        }
    }
}

int main() {
    string str;
    cout<<"Enter string:"<<endl;
    fflush(stdin);
    cin>>str;

    pda(str);
    return 0;
}
